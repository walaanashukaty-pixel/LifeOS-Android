# LifeOS — إصلاح نظام التكرار

تم إعادة بناء منطق التكرار ليكون بسيطًا للمستخدم ومتناسقًا في كل أجزاء التطبيق.

## القواعد النهائية

- **المهام:** مرة واحدة، يوميًا، أسبوعيًا، شهريًا، أو أيام محددة. المهمة المتكررة تبقى سجلًا واحدًا ولا يتم إنشاء نسخ جديدة منها كل يوم.
- **العادات:** التكرار جزء أساسي منها: يوميًا، أيام محددة، أسبوعيًا، أو شهريًا.
- **الأهداف:** لا يوجد تكرار. الهدف يبقى موجودًا حتى يكتمل أو يُحذف، مع موعد نهائي اختياري.
- **الأحداث:** مرة واحدة، يوميًا، أسبوعيًا، شهريًا، سنويًا، أو أيام محددة. قائمة الأحداث القادمة تعرض السلسلة مرة واحدة مع أقرب موعد قادم، بينما التقويم يعرض كل مرات الاستحقاق.

## واجهة أبسط

- تاريخ البداية/المهمة يظهر بصورة طبيعية.
- تاريخ نهاية التكرار اختياري وموجود داخل الخيارات المتقدمة فقط.
- عند اختيار "أيام محددة" تظهر أيام الأسبوع فقط.
- لا يتم إظهار إعدادات غير لازمة للمستخدم.

## الإنجاز اليومي

المهمة أو العادة المتكررة لا تتكرر كسجلات جديدة. بدلاً من ذلك، يتم تحديد استحقاقها لكل تاريخ وتخزين إنجاز ذلك التاريخ بشكل مستقل. لذلك إنجاز الأمس لا يجعل مهمة اليوم منجزة تلقائيًا.

## توحيد المنطق

تم ربط نفس محرك التكرار مع:

- صفحة المهام
- صفحة العادات
- صفحة الأحداث والتقويم
- Dashboard
- Mobile Home
- الإشعارات المحلية
- Daily Review
- Pro Dashboard
- Smart Notifications
- AI Analytics
- LifeOS Agent وحقول الإنشاء/التعديل

## التوافق

- العناصر القديمة التي لا تحتوي على recurrence في المهام/الأحداث تعامل كمرة واحدة.
- العادات القديمة تبقى يومية افتراضيًا عند غياب إعداد التكرار.
- لا يحتاج هذا الإصلاح إلى إنشاء نسخة يومية من البيانات.

## الاختبارات

تم تشغيل مجموعة اختبارات المشروع كاملة بعد الإصلاح: **239 اختبارًا ناجحًا من 239، بدون فشل**.

ملاحظة: لم يتم تنفيذ Vite production build محلي كامل لأن dependencies لم تكن متاحة بالكامل في بيئة العمل، لذلك يبقى GitHub Actions هو فحص البناء الإنتاجي النهائي عند رفع المشروع.


## تثبيت الاستحقاق حسب اليوم
- تبويب المهام والعادات يفتح افتراضيًا على **اليوم**، لذلك العناصر غير المستحقة لا تبدو كأنها مطلوبة يوميًا.
- في تبويب **الكل** يبقى العنصر المتكرر محفوظًا ومرئيًا، لكنه يظهر **غير مفعّل اليوم** عندما لا يطابق الجدول، ولا يمكن تسجيله كمنجز/فائت لذلك اليوم.
- الأسبوعي = مرة واحدة كل أسبوع في نفس يوم تاريخ البداية.
- **أيام محددة من كل أسبوع** = يعمل فقط في الأيام التي يختارها المستخدم.
- الشهري = مرة واحدة كل شهر في نفس رقم يوم البداية، مع استخدام آخر يوم في الشهر إذا كان الشهر أقصر (مثل 31 → 28/29 في فبراير).
- تم توحيد حساب "اليوم" على تاريخ الجهاز المحلي في المهام والعادات والـDashboard والموبايل والـAI لتجنب اختلاف اليوم قرب منتصف الليل.
- الـDashboard والتنبيهات والـAI لا تعتبر العنصر نشطًا إلا عندما يكون مستحقًا في ذلك التاريخ.
