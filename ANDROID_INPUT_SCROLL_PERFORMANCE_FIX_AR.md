# إصلاح Android — تسجيل الدخول، الكيبورد، السكرول والأداء

هذه النسخة تعالج المشاكل التي ظهرت على APK مباشرة:

- إصلاح فتح لوحة مفاتيح Android داخل حقول البريد وكلمة المرور وكل حقول النص.
- إضافة `@capacitor/keyboard` وربط أحداث ظهور/اختفاء الكيبورد بالواجهة.
- فرض `adjustResize` على `MainActivity` أثناء GitHub Actions حتى لا يغطي الكيبورد النوافذ.
- جعل شاشة تسجيل الدخول قابلة للسكرول ومناسبة للشاشات الصغيرة، مع توضيح الدخول بالبريد أو Google.
- إصلاح هيكل ارتفاع التطبيق ليكون المحتوى الداخلي هو منطقة السكرول الفعلية بدل أن يتمدد خارج الشاشة.
- إخفاء شريط التنقل السفلي عند ظهور الكيبورد.
- إزالة السحب Gesture من كامل نافذة النماذج لأنه كان يتداخل مع لمس حقول الإدخال على بعض أجهزة Android.
- جعل التركيز الأول داخل نافذة الإضافة يذهب إلى أول حقل كتابة بدل زر الإغلاق.
- إزالة زر الإغلاق الثاني من نافذة إضافة المهمة؛ بقي زر إغلاق واحد فقط.
- تصغير الشريط العلوي على الموبايل وإزالة مسافة الـ safe-area المكررة التي كانت تدفعه إلى منتصف الشاشة.
- تخفيف مؤثرات الانتقال والـ blur على الموبايل لتقليل الحمل وتحسين الاستجابة.

## GitHub Actions

الـ workflow يشغّل الآن `scripts/patch-android-keyboard.mjs` بعد إنشاء مشروع Android، ثم يكمل بناء APK كما في النسخة السابقة.
