# LifeOS — Critical Mobile UX Fix

هذه النسخة مبنية على `LifeOS-Android-GITHUB-READY1-SDK-FIXED` نفسها، وتم إصلاح المشاكل التي ظهرت أثناء التجربة الفعلية على Android.

## ما تم إصلاحه

### 1) تسجيل الدخول وإنشاء الحساب
- شاشة الدخول أصبحت قابلة للتمرير مع لوحة المفاتيح بدل `overflow:hidden`.
- تبويبا "تسجيل الدخول / إنشاء حساب" أصبحا بعرض متساوٍ وغير مضغوطين.
- أخطاء Supabase الشائعة تظهر للمستخدم برسائل عربية واضحة داخل الشاشة.
- البريد يتم تنظيفه (`trim/lowercase`) قبل الإرسال.
- عند إنشاء حساب يحتاج تأكيد البريد، لا تختفي الشاشة بشكل غامض؛ تظهر شاشة واضحة بعنوان "بقي تأكيد البريد فقط" مع البريد الذي أُرسل إليه الرابط.
- لم يعد التطبيق يفتح جلسة غير مؤكدة.

### 2) السكرول داخل التطبيق
- تم تثبيت ارتفاع App Shell على `100dvh` بدل الاعتماد على `min-height` فقط.
- أضيف `min-height: 0` إلى Flex parent وMain، وهو الإصلاح الأساسي الذي يسمح لـ`overflow-y-auto` بالعمل فعلًا.
- Main أصبح يستخدم `touch-action: pan-y` وتم الحفاظ على native momentum scrolling.
- Pull-to-refresh أصبح أقل عدوانية ولا يمنع الحركة العمودية إلا عند سحب واضح للأسفل من أعلى الشاشة.

### 3) الشريط العلوي
- أزيلت المسافة الخارجية القديمة فوق الـheader.
- الـsafe-area أصبح داخل خلفية الـheader نفسها، لذلك الشريط لا يبدو نازلًا داخل الصفحة.
- تم تخفيف blur والـshadow على Android لتحسين الأداء.

### 4) تكرار علامة X عند إضافة مهمة
كان هناك زر إغلاق من `FormModal` وزر إغلاق ثانٍ داخل `TasksPage` على الموبايل.
- الآن يوجد X واحد فقط على الموبايل.
- زر Tasks الداخلي بقي للديسكتوب فقط.
- تم كذلك إزالة `closeButton` من Sonner Toast حتى لا يظهر X للإلغاء فوق أيقونة خطأ X.

### 5) بطء التطبيق
- أزيل state كان يتحدث في كل frame أثناء السكرول (`scrollProgress`) ويسبب re-render مستمر.
- انتقال الصفحات لم يعد يستخدم `mode="wait"` ولا scale/y animations الثقيلة؛ أصبح fade قصير 100ms.
- FormModal لم يعد draggable من كامل جسم الفورم لأن ذلك كان يتعارض مع scroll ويجعل اللمس ثقيلًا.
- أزيلت blur effects الثقيلة من الأسطح الرئيسية على الموبايل.
- خُففت Splash animations الثقيلة والـinfinite blur.
- خُففت shadows على البطاقات الأساسية في Android.

## الاختبارات
- جميع الاختبارات: **336 / 336 ناجحة**.
- أضيفت اختبارات Regression خاصة بالسكرول، Auth، تكرار X، FormModal performance، وMobile performance.
- فحص TypeScript parser للملفات المعدلة لم يُظهر Syntax/Parse errors.

## Build
تمت محاولة `npm install` داخل بيئة الفحص لكنها تجاوزت المهلة، لذلك لم يتم الادعاء بوجود Production Vite/APK build محلي. GitHub Actions الموجودة في المشروع تبقى طريقة البناء الفعلية.
