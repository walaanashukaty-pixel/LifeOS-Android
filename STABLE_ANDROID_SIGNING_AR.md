# توقيع Android الثابت لـ LifeOS

يستخدم GitHub Actions السر الموجود مسبقاً باسم:

`LIFEOS_DEBUG_KEYSTORE_BASE64`

## الإصلاح الحالي

الإصدار السابق كان يستعيد ملف المفتاح ويقرأ SHA-1 منه، لكن Gradle لم يكن مُجبراً على استخدام نفس الملف عند توقيع APK. لذلك كان ملف `LifeOS-SHA1.txt` يعرض بصمة المفتاح الثابت بينما APK الحقيقي يحمل توقيعاً مختلفاً.

الإصدار الحالي يقوم بثلاث خطوات إلزامية:

1. يستعيد المفتاح الثابت من GitHub Secret إلى مسار خاص داخل مساحة البناء.
2. يربط `android/app/build.gradle` صراحةً بهذا المفتاح عبر `signingConfigs.lifeosDebug` ويستخدمه لبناء نسخة debug.
3. بعد إنشاء APK، يشغّل `apksigner verify --print-certs` على **الـAPK الحقيقي** ويقارن SHA-1 الفعلية مع SHA-1 للمفتاح. إذا اختلفتا يفشل الـWorkflow ولا يرفع APK خاطئاً.

البصمة الثابتة التي يجب أن تبقى مسجلة في Google Cloud هي:

`EB:93:BC:15:53:13:12:07:0A:72:8B:B2:F9:53:47:3F:ED:2B:B5:DB`

لا تحذف Secret الحالي، ولا تنشئ Secret جديداً. فقط ارفع هذه النسخة من المشروع وشغّل Build جديداً.
