# LifeOS — إصلاح طبقة البيانات والسحابة والخصوصية

هذه النسخة تنقل بيانات LifeOS الأساسية من التخزين المحلي الوهمي إلى الـ Edge Function الفعلي، مع الحفاظ على تعديلات Free والتكرار السابقة.

## ما تغيّر

- `src/utils/api.ts` أصبح Cloud-first ويستخدم `fetch()` فعليًا إلى `BASE_URL` مع JWT المستخدم و`apikey` وتاريخ الجهاز المحلي.
- لا يتم تخزين أي بيانات جديدة للمهام/العادات/الأهداف/المالية/اليوميات/الوثائق داخل `lifeos_data`.
- `lifeos_data` بقي فقط كمسار ترحيل لمستخدمي النسخ القديمة، ثم تُحذف المفاتيح التي تم ترحيلها بنجاح.
- ترحيل البيانات القديمة غير هدّام: البيانات الموجودة مسبقًا في السحابة لها الأولوية ولا يتم استبدالها بلقطة محلية قديمة.
- الملفات الجديدة تُرفع Multipart إلى Supabase Storage الخاص `make-3df25961-docs`، وليس Base64 داخل localStorage.
- تسجيل الخروج يمسح التخزين القديم وحالة الإشعارات المحلية حتى لو كان الجهاز بلا إنترنت.
- الجلسة المخبأة Offline لا تُقبل إذا انتهى `exp` في JWT.
- جميع حسابات "اليوم" في الطبقات المتأثرة أصبحت تعتمد التاريخ المحلي للجهاز بدل UTC.
- منطق streak للعادات لا يفسر `YYYY-MM-DD` كمنتصف ليل UTC.
- أضيفت مسارات السيرفر الناقصة: habit categories، المالية، تعديل الوثائق، وحذف اليومية.
- تم توحيد اسم KV إلى `kv_store_3df25961` واسم Storage bucket إلى `make-3df25961-docs`.
- أضيفت سياسات RLS وStorage المناسبة وصلاحيات UPDATE للملفات.
- حُذف الملف القديم غير المستخدم `src/imports/pasted_text/habits.tsx`.
- أضيف `npm run test:data` إلى GitHub Actions لمنع رجوع هذه المشاكل.

## مهم قبل نشر APK جديد

### إذا قاعدة Supabase الحالية موجودة مسبقًا
نفّذ محتوى الملف التالي **مرة واحدة** في SQL Editor للمشروع الحالي:

`supabase/repair_cloud_data_storage.sql`

السبب: تعديل ملف migration تاريخي لا يعيد تشغيله تلقائيًا إذا كان Supabase قد سجله سابقًا كمطبّق.

### Edge Function
أعد نشر الـ Edge Function الحالية التي تخدم المسار:

`/functions/v1/make-server-3df25961`

باستخدام المصدر المحدث:

`supabase/functions/server/index.tsx`

لا تغيّر `BASE_URL` إلا إذا غيّرت اسم الـ Edge Function الفعلي في مشروعك.

## الترحيل من النسخ القديمة

عند أول اتصال بعد تسجيل الدخول:

1. يقرأ التطبيق فقط مفاتيح `lifeos_data` التابعة لنفس `user.id`.
2. يرسل البيانات العادية إلى `/migration/local-store`.
3. يحوّل ملفات Base64 القديمة إلى ملفات فعلية ويرفعها إلى Storage.
4. لا يستبدل بيانات موجودة أصلًا في السحابة.
5. يحذف محليًا فقط ما قَبِلته السحابة بنجاح؛ أي عنصر فشل يبقى ليتكرر ترحيله لاحقًا.

## التحقق

- اختبارات Cloud/Data/Privacy الجديدة موجودة في `tests/data-cloud-security.test.mjs`.
- تشغّل بـ `npm run test:data` وهي مضافة إلى GitHub Action قبل الـbuild.
